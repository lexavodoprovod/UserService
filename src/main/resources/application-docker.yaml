spring:
  datasource:
    url: jdbc:postgresql://user-db:5432/${DB_NAME}
  data:
    redis:
      host: user-redis
      port: 6379
eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka/
server:
  port: 8080